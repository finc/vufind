<? // Don't add START and END comments ?>
<?if (!empty($data)): ?>
    <? if (is_array($data)): ?>
       <?= implode('<br/>', array_map(array($this, 'escapeHtml'), $data)) ?>
    <? else: ?>
       <?= $this->escapeHtml($data) ?>
    <? endif; ?>
<? endif; ?>